@model Sociatis.Models.Infos.InfoMenuViewModel

@if (Model.Items.Count > 0)
{
    <div class="row">
        <div class="small-12 columns">
            <div id="actionBar">
                @Html.DisplayFor(m => m.Items)
            </div>
        </div>
    </div>
}