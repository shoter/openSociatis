@model Sociatis.Models.Congress.CongressCandidatesListViewModel

@if (Model.Count > 0)
{
    @Html.DisplayFor(m => m)
}
else {
    <tr>
        <td colspan="3">
            No candidates for this region.
        </td>
    </tr>
}